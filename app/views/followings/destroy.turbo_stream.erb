<%= turbo_stream.replace "follow-unfollow-button" do %>
  <%= render partial: "users/follow_button", locals: { user: @follower, current_user: @user } %>
<% end %>

<%= turbo_stream.replace "profile-section" do %>
  <%= render partial: "profile/profile_section", locals: { user: @follower } %>
<% end %>
